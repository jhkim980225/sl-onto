# 테스트 결과 보고서 — 선택 객체 LLM Q&A (RAG)

- 일시: 2026-07-09 · 대상: v51 프로덕션(`http://192.168.0.100:30494`) · pyservice v5 · 모델: 사내 vLLM qwen3-32b-finance
- 기능: 그래프에서 객체 선택 → 🤖 질문 탭 → 그 객체의 **관계·근거 문서를 RAG 컨텍스트**로 LLM에 전달 → [R n] 관계 인용이 강제된 답변

## 1. 단위/통합 테스트

| 스위트 | 결과 |
|---|---|
| pyservice (`pytest`) — task=ask 파싱·인용 정규화·빈 답변 에러 포함 | **12 passed** |
| Node (`npm test`) — ask 컨텍스트 4건 포함 전체 | **94 pass / 0 fail** (7 skip = DATABASE_URL 없는 로컬의 DB 테스트) |
| `npx tsc --noEmit` | clean |

## 2. 프로덕션 e2e (실 vLLM 호출)

대상 자동 선정: 관계 최다 item = 헤드램프 어셈블리(IHL, 관계 24), fm = 결로·습기(FMFOG, 관계 62→컨텍스트 상한 30).

| # | 객체 | 질문 | 시간 | 판정 |
|---|---|---|---|---|
| 1 | 헤드램프 어셈블리 | 고장모드와 원인을 관계 근거로 설명 | **85.2s** (첫 생성) | ✅ 고장모드 16종+구성부품 5종 열거, **[R n] 인용 21건 전부 실존 관계 번호** |
| 2 | 〃 (동일 질문 재요청) | 〃 | **0.1s** | ✅ 캐시 히트(`cached:true`) — ai_opinions 테이블 |
| 3 | 결로·습기 | 어떤 프로젝트에서 발생, 어떤 조치? | 34.5s | ✅ 발생 프로젝트·조치 5건([R2] 방열 리브, [R6] 실링 이중화 등) 인용 유효 |
| 4 | 헤드램프 어셈블리 | **"우주선 발사 일정은?"** (컨텍스트 밖) | 13.5s | ✅ "컨텍스트에 정보 없음"으로 거부 — **창작(환각) 금지 준수**, 인용 0건 |

인용 유효성 검증: 답변 본문 `[R n]` + `citedRels` 전부가 서버가 조립한 관계 목록(R1..Rn)에 존재하는지 기계 대조 — **4/4 통과**.

## 3. UI 검증 (프로덕션 브라우저 실구동)

- 노드 선택(검색→히트 클릭) → 탑바 "🤖 질문" → 패널에 "질문 대상: 헤드램프 어셈블리" 표시 ✅
- 예시 질문 칩 3개, textarea, 전송 버튼(빈 입력 시 disabled) ✅
- 답변 렌더: [R n]이 시안 배지로 표시(클릭 시 해당 노드 그래프 포커스), 하단 "근거 관계" 칩(R1 고장→렌즈-하우징 단차 …), "캐시됨" 뱃지 ✅
- 콘솔 에러 0

## 4. 알려진 한계 / 후속

- 첫 생성 60~90초(사내 vLLM 부하) — UI에 안내 문구로 완화, 캐시로 재질문 즉시.
- CASE 1에서 R9(REF_MASTER 계열)만 미인용 — 모델이 고장 나열에 집중한 결과로 답변 품질 문제 아님.
- 관계 30개 초과 객체(결로·습기 62개)는 상한 30까지만 컨텍스트 포함 — 실데이터에서 상한 조정 검토.
- 대화 이력은 세션 로컬(새로고침 시 소멸) — 필요 시 영속화 후속.

## 결론

**PASS.** 선택 객체의 온톨로지 관계가 RAG로 LLM에 전달되고, 답변이 실존 관계만 인용하며, 컨텍스트 밖 질문은 거부한다. 캐시·UI·회귀(스모크) 전부 정상.
